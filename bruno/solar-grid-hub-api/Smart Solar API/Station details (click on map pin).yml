info:
  name: Station details (click on map pin)
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:5281//api/stations/{id}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
