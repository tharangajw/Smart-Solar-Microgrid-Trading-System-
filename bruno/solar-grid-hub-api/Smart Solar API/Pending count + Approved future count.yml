info:
  name: Pending count + Approved future count
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:5281/api/operator/dashboard
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
