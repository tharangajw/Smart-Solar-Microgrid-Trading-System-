info:
  name: Delete Reservation
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/reservations/{booking_id_එක}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
