info:
  name: Pending Reservations
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:5281/api/reservations/pending
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
