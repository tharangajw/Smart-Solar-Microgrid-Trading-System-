info:
  name: Approved future reservations count
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:5281//api/reservations/approved
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
